function cmake-build ()
{
    cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=RelWithDebInfo \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_C_COMPILER=clang \
          "$@" \
          ..
}

function destroy ()
{
    if [[ ! -z $1 ]]; then
        rm -rf $1; mkdir $1; cd $1
    fi
}

function i ()
{
    if [[ ! -z $1 && -d $1 ]]; then
        PATH=$1:$PATH
        export -U PATH
        exec zsh -l
    fi
}

export PATH=$PATH:/suse/bin:/usr/share/bcc/tools:/havana/bin
export LC_ALL=C.UTF-8
export VENV_DIR=/havana/py-virtualenvs

alias iosc="osc -Aibs"
alias open="xdg-open"
alias p3="python3"
