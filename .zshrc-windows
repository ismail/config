# This is very slow on Cygwin
setopt NOCORRECT

# Don't complete dll and exe
zstyle ':completion:*:-command-:*' ignored-patterns '(#i)*.exe' '(#i)*.dll'

export DISPLAY=localhost:0.0
export EXTRA_CLANG_FLAGS="-D_HAS_EXCEPTIONS=0 -target i686-pc-windows-msvc"
export GOPATH=C:/Users/ismail/gocode
alias reset="stty sane && cls"
alias sudo="cygstart --action=runas"
export PATH="/cygdrive/c/SysInternals:$PATH"
