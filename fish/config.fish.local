set -gx NINJA_STATUS='[%f/%t %p %es] '

alias obs="osc build --ccache --cpio-bulk-download --download-api-only"

set PATH /data/binaries/texlive/2020/bin/x86_64-linux /usr/share/bpftrace/tools $PATH
set PATH /usr/share/bcc/tools $PATH
set PATH $PATH /usr/local/cuda/bin

~/bin/sig.py > ~/.signature
